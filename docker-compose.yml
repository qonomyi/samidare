services:
  samidare:
    build: .
    container_name: samidare
    restart: unless-stopped
    user: "1000:1000"
    ports:
      - "3000:3000"
      - "3001:3001"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
